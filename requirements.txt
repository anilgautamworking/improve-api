# Core dependencies - Article fetching and parsing
feedparser>=6.0.10
beautifulsoup4>=4.12.0
lxml>=5.0.0
requests>=2.31.0
aiohttp>=3.9.0
python-dotenv>=1.0.0

# Web scraping for dynamic content (required for modern news sites)
playwright>=1.40.0

# PDF parsing (optional, can be removed if not using PDFs)
pdfplumber>=0.10.0

# AI/LLM for question generation
openai>=1.0.0

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.9

# Dashboard & API
flask>=3.0.0
flask-cors>=4.0.0
bcrypt>=4.0.0
pyjwt>=2.8.0

# Utilities
python-dateutil>=2.8.2

# Development (optional - can be removed in production)
pytest>=7.4.0
black>=23.12.0
flake8>=6.1.0

